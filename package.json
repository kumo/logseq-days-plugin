{
  "name": "logseq-days-plugin",
  "version": "1.0",
  "description": "logseq-days-plugin",
  "main": "dist/index.html",
  "targets": {
    "main": false
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build --no-source-maps index.html --public-url ./"
  },
  "keywords": [],
  "author": "Rob Clarke",
  "license": "MIT",
  "dependencies": {
    "@logseq/libs": "0.0.1-alpha.35",
    "@riversun/simple-date-format": "^1.1.8"
  },
  "logseq": {
    "id": "logseq-days-plugin",
    "title": "logseq-days-plugin",
    "icon": "./images/icon.png"
  },
  "devDependencies": {
    "parcel": "^2.0.0"
  }
}
